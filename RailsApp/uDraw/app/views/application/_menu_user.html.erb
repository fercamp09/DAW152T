<div class="collapse navbar-collapse navbar-right">
  <ul class="nav navbar-nav">
    <li class="dropdown">
      <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Bienvenido <%= current_user.name if current_user %> <span class="caret"></span></a>
      <ul class="dropdown-menu">
        <% if current_user %>
            <li><%= link_to 'Log Out', logout_url , method: :delete %> </li>
            <li><%= link_to 'Edit User', edit_user_url(current_user) %> </li>
        <% else %>
            <li><%= link_to 'Log In', login_url%> </li>
        <% end %>
      </ul>
    </li>
  </ul>
</div>
<a id="userImg" class="navbar-brand navbar-right" href="#">
  <%= image_tag('img.jpg', class: 'img-responsive') %>
</a>
